meta {
  name: Update category
  type: http
  seq: 4
}

put {
  url: {{host}}/categories/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name" : "New name"
  }
}

settings {
  encodeUrl: true
}
